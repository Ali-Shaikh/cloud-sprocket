The Go sidecar binaries are created here by `pnpm run build:sidecar`.

Tauri expects host-specific filenames in the form:

- `cloudsprocketd-x86_64-pc-windows-msvc.exe`
- `cloudsprocketd-aarch64-apple-darwin`
- `cloudsprocketd-x86_64-unknown-linux-gnu`

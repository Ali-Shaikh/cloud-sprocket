version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck

issues:
  # Surface the full backlog while lint is non-blocking in CI.
  max-issues-per-linter: 0
  max-same-issues: 0

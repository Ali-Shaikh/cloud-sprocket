apiVersion: cloudsprocket.recipe/v1
id: azure-keyvault-function-azure
kind: app-deploy
version: 0.1.1
name: Key Vault backed Functions (Azure)
summary: Azure Functions with Key Vault access on a real Azure subscription.
description: |
  Security on Azure: function app with managed identity access to Key Vault.
  Sample demonstrates secret lifecycle.
  
  Requires a cloud Azure profile. floci-az can emulate Key Vault in isolation,
  but not the Function App host this recipe deploys.
providers:
  - azure
tags:
  - azure
  - functions
  - keyvault
  - security
  - cloud-azure
engine:
  type: opentofu
  minVersion: "1.6.0"
local:
  runtimes: []
variableGroups:
  - title: Application
    variables:
      - name: app_name
        widget: text
        help: Lowercase prefix.
      - name: environment
        widget: select
        options: [dev, staging, prod]
      - name: azure_location
        widget: text
      - name: azure_tenant_id
        widget: text
        help: Tenant ID.
  - title: Code
    variables:
      - name: backend_source_dir
        widget: directory
        help: Default sample.
  - title: Advanced
    variables:
      - name: tags
        widget: textarea
outputs:
  - name: key_vault_name
    primary: true
  - name: function_app_name
  - name: resource_group_name
lab:
  difficulty: intermediate
  estimatedMinutes: 20
  objectives:
    - See Key Vault + function with access policy
  steps:
    - id: vault
      title: Inspect Key Vault
      body: Open relevant tab.
      actions:
        - type: open-tab
          tab: azure-keyvault
          focus: "{{ outputs.key_vault_name }}"
